# v0.3 -> v0.4 迁移脚本

## 本次变更功能

为 song 表的 (album_id, name) 添加唯一索引，防止同一专辑下歌曲名重复。

## 说明

- MySQL 唯一索引允许多个 NULL 值（album_id 可为空）
- 同一专辑下歌曲名不能重复

## 执行方式

```bash
mysql -u root -ppassword tp_music < sql/迁移脚本/v0.3-v0.4.sql
```

## 回滚方案

```sql
ALTER TABLE song DROP INDEX uk_album_name;
```
